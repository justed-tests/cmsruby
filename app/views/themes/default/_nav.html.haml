%nav.navbar.navbar-default
  %div.container-fluid
    %div.navbar-header
      %a(class="navbar-brand" href="/")
        \ CMS
    %div(class="collapse navbar-collapse")
      %ul(class="nav navbar-nav")
        - @menus.find_by(name: 'Main Nav').menu_items.each do |item|
          %li
            = link_to item.title, item.url
